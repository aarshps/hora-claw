{
  "version": "1.2.5",
  "highlights": [
    "Removed restrictive Gemini tool allowlist from .gemini/settings.json on the Beeyeswon Windows runtime host to prevent policy-denied tool execution.",
    "Kept sandbox disabled for runtime tool execution flow so Hora-claw can use full web/API/script capabilities without policy gating.",
    "Updated runtime docs and secure-tooling skills to codify no-policy host behavior and reduce future regressions."
  ]
}
